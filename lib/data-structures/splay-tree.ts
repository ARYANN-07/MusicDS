/**
 * Splay Tree (Recently Played) — NOT YET IMPLEMENTED
 *
 * This data structure has been removed from the TypeScript layer.
 * It will be implemented in the C++ backend in a future update.
 * The "Recently Played" section shows a placeholder banner until then.
 *
 * Planned C++ location: backend/data-structures/splay_tree.cpp
 * Planned API endpoints:
 *   POST /api/history/record  — record a played song
 *   GET  /api/history/recent  — get recently played (limit N)
 */
